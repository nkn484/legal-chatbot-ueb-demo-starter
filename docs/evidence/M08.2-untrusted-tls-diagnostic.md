# M08.2 — UNTRUSTED verify-false SOAP discovery diagnostic

## Authorization and isolation

User explicitly authorized one diagnostic-only TLS exception after being informed that certificate
verification failure was a hostname mismatch, not expiry. The implementation is isolated under
`spikes/m08_2/`; production source adapters/runtime remain verify-true and fail-closed.

The diagnostic:

- required an explicit acknowledgement flag;
- loaded only the 32 existing manifest `DISCOVERY_APPROVED` numbers;
- pinned static WSDL/POST targets and `GetListVanBanByListSKH` action;
- used a local `verify=false`, `trust_env=false`, no-redirect, no-retry client;
- never called `GetVanBanById`, REST, ingestion, retrieval, citation or the database;
- did not modify the trusted manifest.

## Validation before execution

- Dedicated spike tests: `13 passed`.
- Ruff/format on spike/tests: PASS.
- `git diff --check`: PASS.

## Measured result

Artifact: `docs/evidence/M08.2-vbqppl-untrusted-discovery.json`.

| Field | Value |
|---|---:|
| Trust level | `UNTRUSTED` |
| TLS verified | `false` |
| WSDL GET calls | 1 |
| SOAP discovery POST calls | 32 |
| Total network calls | 33 |
| Successful candidates | 0 |
| Failures | 32 |
| Normalized failure | `document_not_found` for all 32 |
| Fetch allowed | `false` |
| Ingestion allowed | `false` |
| Citation allowed | `false` |

The trusted manifest remained unchanged, and `git diff` showed no manifest mutation.

## Interpretation

Disabling certificate verification removed the TLS identity check but did not produce any document IDs.
Therefore the failed verified run cannot be explained solely by certificate verification. Possible
external causes include changed SOAP search semantics/data coverage, number-format mismatch, stale
service data or an endpoint that no longer serves the expected current corpus. The diagnostic result is
not official-source evidence and cannot authorize fetch or ingestion.

Phase B remains stopped. Corpus expansion still requires independently verified exact official IDs/URLs
or a separately approved official acquisition architecture.
