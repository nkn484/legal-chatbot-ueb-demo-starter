# Bootstrap Prompt
Read `AGENTS.md`, `README_FIRST.md`, `contracts/*.json`, and `docs/00-*` through `docs/13-*`.
Do not implement features.
Run `python scripts/verify_starter_pack.py`; inspect prerequisites (Python, Git, Docker, OpenCode, Node.js); do not install without USER request; report exact next commands to start M00; stop.
